using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExitGateScript : MonoBehaviour
{
    //Variables de control per obertura
    private bool canOpen = false;
    

    /*Funció que s'activa cada vegada que es fa un generador, 
     * el qual comprova quants falten per poder obrir la porta.
     */
    public void CheckGens()
    {
        //si els gens restants = 0, canOpen = true;
    }
}
